<nav>
  <div class='nav-item'>PetPlace</div>
  <div class='nav-item'><%= link_to "Donate", donations_path %></div>
  <div class='nav-item'><%= link_to "Listings", listings_path %></div>
  <% if user_signed_in? %>
    <% if !current_user.profile.nil? %>
      <div class='nav-greeting'><%= "Hello! #{current_user.profile.name}" %></div>
      <div class='nav-item'><%= link_to "Profile", current_user.profile %></div>
      <% else %>
      <div class='nav-item'><%= link_to "Create Profile", '/profiles/new' %></div>
    <% end %>
    <div class='nav-item'><%= link_to "Sign Out", destroy_user_session_path, method: "delete" %>
  <% else %>
    <div class='nav-item'><%= link_to "Register", new_user_registration_path %></div>
    <div class='nav-item'><%= link_to "Log in", new_user_session_path %></div>
  <% end %>
</nav>