<nav class="nav nav-tabs fixed-top navbar-expand justify-content-center bg-info">
  <div class="navbar-nav">
    <div class="nav-link"><%= link_to "PetPlace", '/' %></div>
    <div class="nav-link"><%= link_to "Donate", donations_path %></div>
    <div class="nav-link"><%= link_to "Listings", listings_path %></div>
    <% if user_signed_in? %>
      <% if !current_user.profile.nil? %>
        <div class="nav-link nav-greeting"><%= "Hello! #{current_user.profile.name}" %></div>
        <div class="nav-link"><%= link_to "Profile", current_user.profile %></div>
        <% else %>
        <div class="nav-link"><%= link_to "Create Profile", '/profiles/new' %></div>
      <% end %>
      <div class="nav-link"><%= link_to "Sign Out", destroy_user_session_path, method: "delete" %>
    <% else %>
      <div class="nav-link"><%= link_to "Register", new_user_registration_path %></div>
      <div class="nav-link"><%= link_to "Log in", new_user_session_path %></div>
    <% end %>
  </div> 
</nav>